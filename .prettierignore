# Generated by drizzle-kit. Reformatting them only makes the next
# `npm run db:generate` dirty the tree again — and CI has been failing on this
# on main, not just here.
src/db/migrations/meta/

node_modules
.next
build
